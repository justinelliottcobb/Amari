name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main, develop, 'release/**']

env:
  RUST_BACKTRACE: 1

jobs:
  test-native:
    name: Test Suite (Native Precision)
    runs-on: ubuntu-latest
    env:
      # Override rust-toolchain.toml and pin nightly around rust-lang/rust# ICEs.
      # Keep matrix.rust as the display key so required check names stay stable.
      RUSTUP_TOOLCHAIN: ${{ matrix.rust == 'nightly' && 'nightly-2026-07-13' || 'stable' }}
    strategy:
      matrix:
        rust: [stable, nightly]
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust == 'nightly' && 'nightly-2026-07-13' || 'stable' }}
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo index
        uses: actions/cache@v5
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo build
        uses: actions/cache@v5
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgmp-dev libmpfr-dev

      - name: Run tests with native precision
        run: cargo test --workspace --exclude amari-discovery --features native-precision

      - name: Run clippy
        run: cargo clippy --workspace --features native-precision -- -D warnings
        if: matrix.rust == 'stable'

      - name: Check formatting
        run: cargo fmt --all -- --check
        if: matrix.rust == 'stable'

      - name: Verify workflow crate configuration
        run: ./scripts/verify-workflow-crates.sh
        if: matrix.rust == 'stable'

      - name: Verify discovery CI sharding
        run: python3 scripts/verify-discovery-ci-sharding.py
        if: matrix.rust == 'stable'

      - name: Verify catalog structural drift
        run: |
          echo "==> Regenerating structural catalog"
          cargo run -p amari-discovery --example generate_catalog -- .
          echo "==> Checking for drift in catalog/generated.json"
          if ! git diff --exit-code amari-discovery/catalog/generated.json; then
            echo ""
            echo "ERROR: catalog/generated.json is out of sync. Run:"
            echo "  cargo run -p amari-discovery --example generate_catalog -- ."
            echo "and commit the updated catalog/generated.json."
            exit 1
          fi
          echo "==> Structural catalog is up to date"
        if: matrix.rust == 'stable'

  test-wasm-compat:
    name: Test Suite (WASM-Compatible Precision)
    runs-on: ubuntu-latest
    env:
      RUSTUP_TOOLCHAIN: stable
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-wasm-compat-${{ hashFiles('**/Cargo.lock') }}

      - name: Run tests with WASM-compatible precision
        run: cargo test --workspace --exclude amari-discovery --features high-precision

      - name: Test individual crates
        run: |
          cargo test -p amari-core --features high-precision
          cargo test -p amari-tropical --features high-precision
          cargo test -p amari-dual --features high-precision
          cargo test -p amari-network --features high-precision
          cargo test -p amari-relativistic --features high-precision

  wasm:
    name: WASM Build
    runs-on: ubuntu-latest
    env:
      RUSTUP_TOOLCHAIN: stable
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install and verify WASM target
        run: |
          rustup target add wasm32-unknown-unknown
          rustup target list --installed
          rustup show

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-wasm32-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-wasm32-
            ${{ runner.os }}-cargo-

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Check WASM compilation (individual crates)
        run: |
          echo "🔍 Checking WASM target availability..."
          rustc --print target-list | grep wasm32

          echo "🔍 Checking sysroot..."
          rustc --target wasm32-unknown-unknown --print sysroot

          echo "🔍 Testing basic WASM compilation..."
          echo 'fn main() {}' > test.rs
          rustc --target wasm32-unknown-unknown test.rs -o test.wasm
          rm test.rs test.wasm

          echo "✅ Basic WASM compilation works, checking crates..."

          echo "🔍 Checking amari-core..."
          cargo check -p amari-core --target wasm32-unknown-unknown --features high-precision -v

          echo "🔍 Checking amari-tropical..."
          cargo check -p amari-tropical --target wasm32-unknown-unknown --features high-precision -v

          echo "🔍 Checking amari-dual..."
          cargo check -p amari-dual --target wasm32-unknown-unknown --features high-precision -v

          echo "🔍 Checking amari-network..."
          cargo check -p amari-network --target wasm32-unknown-unknown --features high-precision -v

          echo "🔍 Checking amari-relativistic..."
          cargo check -p amari-relativistic --target wasm32-unknown-unknown --features high-precision -v

      - name: Build WASM package
        run: |
          cd amari-wasm
          echo "🏗️ Building WASM package with wasm-pack..."

          # Try standard build first
          if wasm-pack build --target web --out-dir pkg --scope amari --verbose; then
            echo "✅ Standard WASM build succeeded"
          else
            echo "⚠️ Standard build failed, trying alternative approaches..."

            # Clean target directory to avoid cache issues
            cargo clean

            # Try with explicit target installation and rebuild
            echo "🔄 Reinstalling WASM target and trying again..."
            rustup target remove wasm32-unknown-unknown || true
            rustup target add wasm32-unknown-unknown

            # Verify target is properly installed
            rustup target list --installed | grep wasm32-unknown-unknown

            # Try build again
            if wasm-pack build --target web --out-dir pkg --scope amari --verbose; then
              echo "✅ WASM build succeeded after target reinstall"
            else
              echo "❌ WASM build failed even after target reinstall"
              echo "🔍 Target list:"
              rustup target list --installed
              echo "🔍 Toolchain info:"
              rustup show
              exit 1
            fi
          fi

      - name: Test WASM package
        run: |
          cd amari-wasm
          wasm-pack test --node

      - name: Upload WASM artifacts
        uses: actions/upload-artifact@v7
        with:
          name: wasm-package
          path: amari-wasm/pkg/

      - name: Check WASM surface drift
        run: |
          echo "==> Regenerating WASM discovery surface"
          bash scripts/generate-discovery-wasm-surface.sh
          echo "==> Checking for drift in catalog/generated-wasm.json"
          if ! git diff --exit-code amari-discovery/catalog/generated-wasm.json; then
            echo ""
            echo "ERROR: catalog/generated-wasm.json is out of sync with the authoritative"
            echo "wasm-bindgen surface. Run scripts/generate-discovery-wasm-surface.sh and"
            echo "commit the updated catalog/generated-wasm.json."
            exit 1
          fi
          echo "==> WASM surface catalog is up to date"

  msrv:
    name: MSRV Check (1.89)
    runs-on: ubuntu-latest
    env:
      RUSTUP_TOOLCHAIN: 1.89.0
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@1.89.0

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-msrv-${{ hashFiles('**/Cargo.lock') }}

      # Compile-only floor: nalgebra 0.35 / safe_arch 1.1 / wide 1.6.1 require
      # rustc 1.89, so the workspace rust-version is 1.89 (raised from the
      # unenforced 1.75 declaration in the 0.25 rewrite baseline).
      - name: Check workspace at MSRV
        run: cargo check --workspace

  test-z3-vendored:
    name: Rewrite SMT (Vendored Z3)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      RUSTUP_TOOLCHAIN: stable
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-z3-${{ hashFiles('**/Cargo.lock') }}

      # Vendored Z3 compiles the solver from source via CMake into target/;
      # cache the full build output keyed by compiler and lockfile so only
      # lockfile changes rebuild Z3 (cold build ~4-5 min on hosted runners).
      - name: Cache vendored Z3 build
        uses: actions/cache@v5
        with:
          path: target
          key: ${{ runner.os }}-z3-vendored-${{ hashFiles('**/Cargo.lock') }}-v1

      - name: Test rewrite smt feature
        run: cargo test -p amari-rewrite --features smt

  benchmark:
    name: Performance Benchmarks
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    env:
      RUSTUP_TOOLCHAIN: stable
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run benchmarks
        run: cargo bench --workspace 2>&1 | tee output.txt || echo "Benchmarks completed"
        continue-on-error: true

      - name: Store benchmark result
        uses: benchmark-action/github-action-benchmark@v1
        with:
          tool: 'cargo'
          output-file-path: output.txt
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: true
        continue-on-error: true
        if: always()